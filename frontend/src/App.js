import React from 'react';

function App() {
    return (
        <div style={{ padding: 24 }}>
            <h1>Synthetic Data Studio (Frontend)</h1>
            <p>Frontend placeholder. Connects to backend at {process.env.REACT_APP_API_URL}</p>
        </div>
    );
}

export default App;
