# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Disable Turbopack (use webpack for stable HMR)
# Run with: npm run dev (already configured in package.json)

# Optional: Enable verbose logging for debugging
# NEXT_TELEMETRY_DISABLED=1
